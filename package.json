{
  "name": "minclaw",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fmt": "prettier --write \"**/*.{ts,json}\" --ignore-path .prettierignore",
    "test": "pnpm -r test",
    "dev:host": "pnpm --filter minclaw-host dev",
    "dev:agent": "pnpm --filter minclaw-agent dev",
    "build": "agent/build.sh",
    "start": "docker compose up -d && pnpm dev:host",
    "stop": "docker compose down && pkill -f 'minclaw-host' 2>/dev/null; lsof -ti :13821 | xargs kill -9 2>/dev/null; true",
    "reload": "pnpm stop && pnpm build && pnpm start",
    "clear-jobs": "cd host && node --require tsx/cjs scripts/clear-jobs.ts"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
